## 1. Attribution and presentation data

- [x] 1.1 Add persistent local display names and guarded update/reset API; verify validation, unknown machine and cross-origin rejection with synthetic tests.
- [x] 1.2 Apply local names consistently to dashboard queries while preserving raw exports; verify persistence, filtering, provider independence, original payloads and repeat-import idempotence.
- [x] 1.3 Replace OS-derived new member defaults and preserve existing portable machine attribution; verify no scan and existing-machine compatibility tests.

## 2. Usable interface

- [x] 2.1 Add Sources display-name editing/reset and provenance explanation; verify browser save, refresh, filter, reset and local-only export behavior.
- [x] 2.2 Replace prominent UUIDs with observed session time and member/computer context; verify keyboard-accessible IDs, real provider badges and no invented titles/prompts.

## 3. Acceptance

- [x] 3.1 Run check, production build and browser acceptance with synthetic data; independently inspect desktop/375px pages and session/form crops including long names.
- [x] 3.2 Record actual findings, commands, evidence and limits in build-log; update product docs, mark verified tasks and restart the user's local app.
