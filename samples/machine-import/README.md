# Synthetic machine import fixtures

These fictional sessions exercise the browser's one-click collector. Playwright
sets both provider home variables to these directories before starting its server,
so browser tests never collect from the developer's real provider directories.
The files contain two usage events and two eligible human prompts in total.
