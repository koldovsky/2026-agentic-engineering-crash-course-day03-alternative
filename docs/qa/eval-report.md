# Eval Report (generated by the eval-suite workflow — do not hand-edit)

The quality BAR for graded behavior. Recordings illustrate these cases for humans; this report decides pass/fail. Guarded in CI by `node scripts/check-eval-ratchet.mjs` against `quality/eval-baseline.json`.

- Cases: 2 (2 pass, 0 fail)
- Pass mark: 70/100 per case; CRITICAL rubric misses fail a case outright.
- Per-dimension score (ratcheted): {"usability-clarity":92.5}

| Case | Dimension | Proves | Score | Verdict | Judges |
|---|---|---|---|---|---|
| csv-empty-filtered | usability-clarity | FR-CSV-03 | 93 | pass | 1 |
| csv-empty-source | usability-clarity | FR-CSV-03 | 92 | pass | 1 |

## Per-case notes

### csv-empty-filtered — 93/100 (pass)
The output states the exact condition from the rubric ("no usage matches the current filters") and both remedies (clear or widen), satisfying the gating criterion. It is exactly two sentences, uses only vocabulary visible on the card (filter names, CSV, usage), and ends with a direct imperative telling the user what to do. The only deductions are stylistic: the trailing comma before "to include usage in the CSV" is slightly awkward, and "widen the provider ... range" is a small grammatical stretch since only the date filter is truly a range; neither is a rubric miss.

### csv-empty-source — 92/100 (pass)
The output correctly identifies the empty-source condition (\"this source has no usage yet\"), gives the right remedy (\"Import usage in Data sources\"), and never suggests widening filters, satisfying the gating criterion. It is exactly two sentences, avoids internal identifiers, and the next step is concrete and located in the UI. The only softness is the trailing clause \"to get a model breakdown\", which explains the payoff slightly obliquely for a disabled Download CSV control, but it does not violate any rubric item, so a small deduction from a shippable score.
